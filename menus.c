#include "menus.h"

void displayMainMenu()
{
	if (puts("How do you want to log in? (Select one of the 3 options below)\n")){}
	if (puts("1. Doctor")){}
	if (puts("2. Nurse")){}
	if (puts("3. Administrator\n")){}
	if (puts("Or enter 4 to exit the program\n")){}
}
